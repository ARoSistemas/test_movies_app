import 'dart:ui' show Color;

/// Defines custom colors for specific use cases in the application.
///
/// This class provides a centralized location for managing color constants,
/// ensuring consistency across the app's UI.

class CustomColors {
  /// The color used to represent favorite items.
  static const Color favoriteRed = Color(0xFFE57373);

  /// The color used to represent ratings.
  static const Color ratingYellow = Color(0xFFFFD54F);

  /// The background color for cards.
  static const Color cardBackground = Color(0xFFFFFFFF);

  /// The border color for movie posters.
  static const Color moviePosterBorder = Color(0xFF81C784);

  /// The base color for shimmer effects.
  static const Color shimmerBase = Color(0xFFE8F5E8);

  /// The highlight color for shimmer effects.
  static const Color shimmerHighlight = Color(0xFFFFFFFF);
}
